import { styled } from "styled-components"

export const ContactSection_Styled = styled.section`
	width: 100%;
	min-height: 100vh;
	background-color: var(--bg-color-primary);
	color: var(--white-color);
	margin-top: var(--header-height);

	& h1 {
		padding: 30px;
		text-align: center;
		font-size: 1.7rem;
	}
`
